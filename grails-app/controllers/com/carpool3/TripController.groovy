package com.carpool3


class TripController {
     def search(){
         if (params.available) {
         System.out.println("YES")}
         //def userList =Trip.findAllByDateAndFromCityAndToCity(params.date, params.fromcity,params.tocity)
     //[Instance:userList]
     }
    def index() {}
}
